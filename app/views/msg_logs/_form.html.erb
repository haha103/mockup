<%= form_for(@msg_log) do |f| %>
  <% if @msg_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@msg_log.errors.count, "error") %> prohibited this msg_log from being saved:</h2>

      <ul>
      <% @msg_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recorded_at %><br>
    <%= f.datetime_select :recorded_at %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
