<%= form_for(@msg_type) do |f| %>
  <% if @msg_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@msg_type.errors.count, "error") %> prohibited this msg_type from being saved:</h2>

      <ul>
      <% @msg_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
