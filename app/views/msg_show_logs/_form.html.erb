<%= form_for(@msg_show_log) do |f| %>
  <% if @msg_show_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@msg_show_log.errors.count, "error") %> prohibited this msg_show_log from being saved:</h2>

      <ul>
      <% @msg_show_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :collected_at %><br>
    <%= f.datetime_select :collected_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
