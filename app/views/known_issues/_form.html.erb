<%= form_for(@known_issue) do |f| %>
  <% if @known_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@known_issue.errors.count, "error") %> prohibited this known_issue from being saved:</h2>

      <ul>
      <% @known_issue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
